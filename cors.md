# CORS(Cross-Origin Resource Sharing)

<br />
<br />

* CORS란?

---

```
CORS는 웹 브라우저가 다른 출처의 서버로 HTTP 요청을 보낼 때,
보안 정책으로 인해 발생하는 제한을 관리하는 방법이다.

브라우저는 기본적으로 SOP(Same-Origin Policy)를 따라 동일한 출처에서만 리소스를 요청할 수 있다.

하지만 CORS를 사용하면 서버가 특정 출처의 요청을 허용하도록 설정할 수 있다.
```

<br />
<br />
<br />
<br />

1. 출처 (Origin)란?

```
프로토콜 + 호스트 + 포트 조합이다. 

예시 -> http://127.0.0.1:3000
```

<br />
<br />
<br />

2. 브라우저의 입장

```
페이지랑 API 출처가 다르면, 함부로 응답을 JS에 안 줌
-> 악성 사이트가 내 정보로 로그인된 다른 사이트 API를 몰래 호출해서 결과를 읽는 걸 막기위함

요청 자체는 서버까지 갈 수 있어도, 브라우저가 JS에게 결과를 안 넘겨 줌
-> Same-Origin Policy
```

<br />
<br />
<br />

3. 서버의 입장

```
CORS의 Origin을 http://127.0.0.1:3000으로 등록해두었다면,

API 서버가 응답 헤더로 이렇게 말함
-> http://127.0.0.1:3000에서 온 요청은 괜찮아. 그 페이지 JS에게 응답 줘도 됨 -> 해당 출처로 응답

"한 마디로 서버가 이 프론트는 허용"
```
