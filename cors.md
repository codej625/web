# CORS(Cross-Origin Resource Sharing)

<br />
<br />

* CORS란?

---

```
"브라우저"가 다른 오리진의 리소스를 JS가 읽어도 되는가? 
-> 서버 허락 하에 통제하는 것

a.com이 보낸 응답을 b.com의 JS가 읽는 것은 X
-> 기본적으로(CORS) a.com은 a.com에 JS 읽을 수 있음.

즉, CORS는 브라우저가 기본으로 막고 있는 걸 서버가 "풀어주는" 정책이다.

주체가 "브라우저" 허용해주는건 "서버"
```

<br />
<br />
<br />
<br />

1. 출처 (Origin)란?

```
프로토콜 + 호스트 + 포트 조합이다. 

예시 -> http://127.0.0.1:3000
```

<br />
<br />
<br />

2. 간단 정리

| 구분 | 내용 |
|---|---|
| 주체 | 브라우저가 강제하는 정책 |
| 막는 대상 | 응답(response)을 JS가 읽는 것 |
| 설정 위치 | 서버가 `Access-Control-Allow-Origin` 등 헤더로 허용 |
| 기본 상태 | 기본적으로 막혀있음 (동일 출처만 허용) |
| 우회 가능? | 서버가 헤더로 "허용"하면 우회(=정상 허용) |
